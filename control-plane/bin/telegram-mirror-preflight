#!/bin/bash
set -euo pipefail

ROOT="${TELEGRAM_CONTROL_PLANE_ROOT:-./control-plane}"
export PYTHONPATH="${ROOT}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m telegram_control_plane mirror-preflight "$@"
