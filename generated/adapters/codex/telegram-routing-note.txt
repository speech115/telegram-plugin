For Telegram tasks: use the `telegram` skill. For live reads (today/recent/search), run `tg` first — do not require `@telegram`. Use direct full MCP for media, sends, and fallback. For sends with explicit target and exact text, prefer `telegram_send` / `send_message`; preview/confirmed tools are optional.
