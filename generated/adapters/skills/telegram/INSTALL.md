# Telegram Skill Install

Install the `telegram` skill directory into the host skill root. After installation, use natural language such as `read today's Telegram chat with ...`; do not require users to invoke `@telegram`.
