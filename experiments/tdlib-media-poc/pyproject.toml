[project]
name = "tdlib-media-poc"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Pytdbot>=0.10.1",
    "python-dotenv>=1.0.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.uv]
package = false
